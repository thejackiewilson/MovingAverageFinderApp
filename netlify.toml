 [build]
  command = "pip install -r requirements.txt && python build.py"
  publish = "build"

[build.environment]
  PYTHON_VERSION = "3.8.1"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
